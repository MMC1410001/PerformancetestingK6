module.exports = {
  stopOnFailure: true,   // Set to false if we need to continue running the files even after failure is observed
  testFolder: './tests',
  outputFolder: './reports',
  generateCharts: true // ⬅️ Enable chart generation
};
